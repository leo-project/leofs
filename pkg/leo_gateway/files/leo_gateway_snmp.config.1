[{snmp, [{agent, [{versions, [v1, v2]},
                  {db_dir, "/var/db/leo_gateway/snmp"},
                  {config, [{dir, "snmp/snmpa_gateway_1/agent/conf"},
                            {verbosity, silence}]},
                  {net_if, [{verbosity, silence}]}
                 ]},
         {verbosity, silence}]}
].
