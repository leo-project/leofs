[{snmp, [{agent, [{versions, [v1, v2]},
                  {db_dir, "snmp/snmpa_manager_2/db"},
                  {config, [{dir, "snmp/snmpa_manager_2/agent/conf"},
                            {verbosity, silence}]},
                  {net_if, [{verbosity, silence}]}
                 ]},
         {verbosity, silence}]}
].
